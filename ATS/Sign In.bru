meta {
  name: Sign In
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/signin
  body: json
  auth: inherit
}

body:json {
  {
    "email": "devmastersinstitue@gmail.com",
  "password": "123"
  }
}

vars:pre-request {
  baseUrl: http://localhost:8080
}

settings {
  encodeUrl: true
  timeout: 0
}
